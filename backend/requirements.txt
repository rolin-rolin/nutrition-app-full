# Core web framework
fastapi>=0.115.12,<0.116.0
uvicorn[standard]==0.24.0

# AI and ML libraries (installed separately in Dockerfile for compatibility)
sentence-transformers==3.0.0
openai>=1.79.0,<2.0.0

# LangChain (only what we actually use)
langchain>=0.3.26,<0.4.0
langchain-community>=0.3.26,<0.4.0
langchain-openai>=0.3.27,<0.4.0
langchain-chroma>=0.2.4,<0.3.0

# Vector database
chromadb>=1.0.13,<2.0.0

# Data processing (only what we use)
pyyaml>=6.0.2,<7.0.0

# Database (SQLite only)
sqlalchemy>=2.0.41,<3.0.0

# Data validation
pydantic>=2.11.4,<3.0.0

# HTTP client
httpx>=0.28.1,<0.29.0

# Environment and configuration
python-dotenv>=1.1.0,<2.0.0

# Token counting for OpenAI
tiktoken>=0.9.0,<0.10.0

